#include <gtest/gtest.h>

TEST(test1222, test1_212true_is_true)
{
	EXPECT_TRUE(true);
	EXPECT_TRUE(true);
	EXPECT_TRUE(true);
	// EXPECT_TRUE(false); // uncheck if you want to test errors in testing
	EXPECT_TRUE(true);
}